{
  "name": "robertbrockie.github.io",
  "version": "1.0.0",
  "description": "Brock's Bodybuilding Journal",
  "scripts": {
    "dev": "bundle exec jekyll serve",
    "dev:docker": "docker run --rm --volume=\"$PWD:/srv/jekyll\" -p 4000:4000 -it jekyll/jekyll jekyll serve",
    "build": "bundle exec jekyll build",
    "build:docker": "docker run --rm --volume=\"$PWD:/srv/jekyll\" -it jekyll/jekyll jekyll build",
    "log": "node scripts/log-workout.js",
    "backfill": "node scripts/backfill-weights.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
